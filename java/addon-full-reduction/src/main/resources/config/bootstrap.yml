server:
  port: 8798
spring:
  main:
    allow-circular-references: true
  application:
    name: addon-full-reduction
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 124.222.25.145:8884
      discovery:
        namespace: ${spring.profiles.active}
        ip: 192.168.1.246
      config:
        namespace: ${spring.profiles.active}
        file-extension: yml
        shared-configs:
          - dataId: application-common.${spring.cloud.nacos.config.file-extension}
          - dataId: addon.${spring.cloud.nacos.config.file-extension}