import { RoomRoleJointType } from '@/q-plugin/liveStream/views/types'
export interface ApiRole {
    shopName: string
    createTime: string
    id: string
    role: RoomRoleJointType
    shopId: string
    userName: string
    wechatNumber: string
}
