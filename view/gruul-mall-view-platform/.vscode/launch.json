{
    "configurations": [
        {
            "name": "Attach to Chrome",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:8000/platform/#/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
